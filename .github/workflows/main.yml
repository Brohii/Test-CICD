name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: centos-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.CICD_TESTING_KEY }}

    - name: Deploy to Server
      run: |
        scp -r ./CICD-TESTEING/* ubuntu@ec2-54-175-115-251:/etc/CICDTESTING
